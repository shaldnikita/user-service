version: '2.0'


services:
  db:
    image: mysql:5.7.11
    restart: always
    ports:
        - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: 123
      MYSQL_DATABASE: userservice

  adminer:
    image: adminer
    restart: always
    ports:
        - 8080:8080

  userservice:
     image: shaldnikita/userservice
     restart: always
     ports:
        - 9000:8080
     environment:
        MYSQL_ROOT_PASSWORD : 123
        MYSQL_DATABASE: userservice

